# about this shit
This Script Forked From https://www.kali.org/docs/nethunter/nethunter-rootless/ but this is minimal version of that

# recomended after install
$ sudo apt update && sudo apt upgrade

# singgle install

$ termux-setup-storage && pkg install git  && pkg install wget && git clone https://github.com/H1kikomoR1/kali-linux-minimal && cd kali-linux-minimal && chmod +x nethunter && ./nethunter
